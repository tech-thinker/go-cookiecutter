version: "3"

services: 
    gomvc:
        networks: 
            - gomvc-net
        build: .
        container_name: gomvc
        env_file: .env
        ports: 
            - "3000:3000"
        volumes: 
            - "../:/go/src/app"
networks: 
    gomvc-net: